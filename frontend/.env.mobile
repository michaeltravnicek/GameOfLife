# Mobile (Capacitor) build: the SPA is bundled in the app and calls the
# production API cross-origin. Used by `npm run build:mobile` (--mode mobile).
VITE_API_URL=https://www.gameofyolo.com/api
VITE_PUBLIC_WEB_URL=https://www.gameofyolo.com
